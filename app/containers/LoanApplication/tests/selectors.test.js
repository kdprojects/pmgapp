// import { fromJS } from 'immutable';
// import { makeSelectLoanApplicationDomain } from '../selectors';

// const selector = makeSelectLoanApplicationDomain();

describe('makeSelectLoanApplicationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
